import java.util.Scanner;

public interface Operaciones {
    void solucion(Scanner scanner, double [][] matriz);
    void matriz(double [][] matriz);
    public double [][] ingresa(Scanner scanner,double [][] matriz);
}
